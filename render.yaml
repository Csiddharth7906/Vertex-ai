services:
  - type: web
    name: vertex-ai-chatbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
      - key: GROQ_API_KEY
        sync: false
